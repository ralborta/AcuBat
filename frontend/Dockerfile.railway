# Dockerfile simple para Railway - Frontend
FROM node:18-alpine

WORKDIR /app

# Copiar package.json
COPY package*.json ./
RUN npm install

# Copiar código fuente
COPY . .

# Build
RUN npm run build

# Exponer puerto
EXPOSE 8080

# Comando de inicio
CMD ["npm", "start"]
